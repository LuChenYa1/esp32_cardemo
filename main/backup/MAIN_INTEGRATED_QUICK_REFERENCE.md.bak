# main_integrated.c 快速参考

## 快速启动

### 1. 编译和烧录
```bash
# 编译项目
idf.py build

# 烧录到ESP32
idf.py flash

# 查看日志
idf.py monitor

# 或一次性执行
idf.py build flash monitor
```

### 2. 切换到集成版本

编辑 `main/CMakeLists.txt`，确保使用 `main_integrated.c`：

```cmake
idf_component_register(SRCS "main_integrated.c" ...
```

注释掉其他主文件（如 `main.c`、`main_integrated_test.c` 等）。

## 系统架构一览

```
┌─────────────────────────────────────────────────────────┐
│                    ESP32 系统架构                        │
├─────────────────────────────────────────────────────────┤
│  硬件中断层（最高优先级）                                │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │ Timer 0 (1ms)   │  │ Timer 1 (10ms)  │              │
│  │ - 灰度扫描      │  │ - PD控制        │              │
│  │ - 转弯检测      │  │ - 转弯状态机    │              │
│  └─────────────────┘  └─────────────────┘              │
├─────────────────────────────────────────────────────────┤
│  FreeRTOS任务层（低优先级）                              │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐   │
│  │ ADC采样任务  │ │ 数码管显示   │ │ 舵机控制     │   │
│  │ 优先级: 5    │ │ 优先级: 3    │ │ 优先级: 3    │   │
│  │ 周期: 1ms    │ │ 周期: 200ms  │ │ 周期: 3s     │   │
│  └──────────────┘ └──────────────┘ └──────────────┘   │
├─────────────────────────────────────────────────────────┤
│  共享数据层（原子变量保护）                              │
│  - ADC缓存值 (g_adc_left_cached, g_adc_right_cached)   │
│  - 转弯标志 (g_turn_type, g_turning_in_progress)       │
│  - 状态机状态 (g_turn_state)                            │
└─────────────────────────────────────────────────────────┘
```

## 初始化流程

```
1. 硬件外设初始化
   ├─ PWM (电机控制)
   └─ 灰度传感器 (ADC2)

2. 软件模块初始化
   ├─ 转弯检测模块
   ├─ PD控制器
   └─ 转弯状态机

3. 系统自检
   ├─ ADC读取测试
   └─ 电机PWM测试

4. 看门狗配置 (5秒超时)

5. 定时器系统初始化

6. FreeRTOS任务创建
   ├─ ADC采样任务
   ├─ 数码管显示任务
   └─ 舵机控制任务

7. 启动定时器系统 ✓
```

## 参数配置

### 灰度传感器校准
```c
LEFT_WHITE_VALUE    = 4095  // 左传感器白色值
LEFT_BLACK_VALUE    = 1476  // 左传感器黑色值
RIGHT_WHITE_VALUE   = 4095  // 右传感器白色值
RIGHT_BLACK_VALUE   = 1546  // 右传感器黑色值
```

### PD控制参数
```c
DEFAULT_SPEED = 700   // 基础速度 (0-1023)
DEFAULT_KP    = 5.0   // 比例系数
DEFAULT_KD    = 12.0  // 微分系数
```

### 转弯检测阈值
```c
LEFT_THRESHOLD  = (4095 + 1476) / 2 = 2785
RIGHT_THRESHOLD = (4095 + 1546) / 2 = 2820
CONFIRM_COUNT   = 15  // 连续15次确认
```

## GPIO引脚分配

| 功能 | GPIO | 说明 |
|------|------|------|
| 左灰度传感器 | GPIO19 | ADC2_CH9 |
| 右灰度传感器 | GPIO20 | ADC2_CH7 |
| 电机1正向 | GPIO2 | LEDC PWM CH0 |
| 电机1反向 | GPIO3 | LEDC PWM CH1 |
| 电机2正向 | GPIO4 | LEDC PWM CH2 |
| 电机2反向 | GPIO5 | LEDC PWM CH3 |
| 电机3正向 | GPIO6 | LEDC PWM CH4 |
| 电机3反向 | GPIO7 | LEDC PWM CH5 |
| 电机4正向 | GPIO8 | LEDC PWM CH6 |
| 电机4反向 | GPIO9 | LEDC PWM CH7 |
| TM1637 CLK | GPIO34 | 数码管时钟 |
| TM1637 DIO | GPIO33 | 数码管数据 |
| DHT11 | GPIO38 | 温湿度传感器 |
| 触摸传感器 | GPIO37 | 数字输入 |
| UART0 TX | GPIO1 | RS485舵机发送 |
| UART0 RX | GPIO3 | RS485舵机接收 |

## 运行时监控

系统每5秒打印一次状态信息：

```
系统状态 - Timer0: 5000次/5s, Timer1: 500次/5s, ADC错误: 0次
转弯状态 - 状态机: 0, 转弯类型: 0, 转弯中: 0
```

### 正常值范围
- **Timer 0**: 4500-5500次/5秒（1ms周期）
- **Timer 1**: 450-550次/5秒（10ms周期）
- **ADC错误**: <50次/5秒

### 异常警告
- Timer频率超出范围 → 检查系统负载
- ADC错误率过高 → 检查硬件连接
- ADC连续失败100次 → 自动进入安全模式（停止所有电机）

## 调试技巧

### 1. 查看详细日志
```c
// 在 app_main() 开头添加
esp_log_level_set("*", ESP_LOG_DEBUG);
```

### 2. 禁用某个任务
```c
// 注释掉不需要的任务
// ret = display_task_create();
// TaskHandle_t servo_handle = servo_task_create();
```

### 3. 调整PD参数
```c
// 在 init_software_modules() 中修改
pd_controller_set_params(
    800,    // 速度：增加到800
    6.0f,   // Kp：增加到6.0
    15.0f   // Kd：增加到15.0
);
```

### 4. 修改转弯确认次数
```c
// 在 turn_detector.h 中修改
#define CONFIRM_COUNT 10  // 从15改为10
```

## 常见问题

### Q1: 编译错误 "undefined reference to xxx"
**A**: 检查 `main/CMakeLists.txt` 是否包含所有必需的组件：
```cmake
REQUIRES timer_system turn_detector pd_controller 
         turn_statemachine display_task servo_task ...
```

### Q2: 系统启动后立即重启
**A**: 可能是看门狗超时，检查：
1. IDLE任务是否有足够CPU时间
2. 中断函数是否执行时间过长
3. 是否有死循环或阻塞调用

### Q3: ADC读取失败
**A**: 检查：
1. WiFi是否已禁用（ADC2与WiFi冲突）
2. GPIO19/20是否正确连接
3. 传感器供电是否正常

### Q4: 电机不转
**A**: 检查：
1. PWM初始化是否成功
2. GPIO2-9是否正确连接
3. 电机驱动板供电是否正常
4. PD控制参数是否合理

### Q5: 数码管不显示
**A**: 检查：
1. I2C初始化是否成功
2. TM1637连接是否正确（GPIO33/34）
3. display_task是否正常运行

### Q6: 舵机不动
**A**: 检查：
1. UART0是否被日志占用（需要关闭日志输出）
2. RS485连接是否正确
3. 舵机供电是否正常
4. 舵机ID是否匹配（0x02-0x05）

## 性能优化

### 1. 减少日志输出
```c
// 关闭所有日志（提高性能）
esp_log_level_set("*", ESP_LOG_NONE);

// 或只保留错误日志
esp_log_level_set("*", ESP_LOG_ERROR);
```

### 2. 调整任务优先级
```c
// 如果ADC采样任务抢占过多CPU
// 降低优先级从5到4
```

### 3. 优化中断执行时间
- Timer 0中断应在200微秒内完成
- Timer 1中断应在2毫秒内完成
- 避免在中断中使用浮点运算

## 扩展功能

### 添加语音控制
```c
// 在 create_freertos_tasks() 中添加
#include "voice_module.h"
voice_module_init();
```

### 添加蓝牙控制
```c
// 在 init_hardware_peripherals() 中添加
#include "ble.h"
ble_init();
```

### 添加超声波避障
```c
// 在 create_freertos_tasks() 中添加
#include "hc_sr04.h"
xTaskCreate(ultrasonic_task, "ultrasonic", 2048, NULL, 3, NULL);
```

## 相关文档

- **设计文档**: `.kiro/specs/stm32-to-esp32-migration/design.md`
- **需求文档**: `.kiro/specs/stm32-to-esp32-migration/requirements.md`
- **任务列表**: `.kiro/specs/stm32-to-esp32-migration/tasks.md`
- **实施总结**: `main/TASK16_1_IMPLEMENTATION_SUMMARY.md`

## 技术支持

如有问题，请检查：
1. 硬件连接是否正确
2. 供电是否稳定
3. ESP-IDF版本是否兼容
4. 组件依赖是否完整

祝使用愉快！🚗
