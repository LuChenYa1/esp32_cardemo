# 任务16.2：真实硬件集成测试指南

## 概述

本文档提供在真实ESP32硬件上执行集成测试的详细步骤和验证清单。

## 测试前准备

### 1. 硬件准备清单

- [ ] ESP32开发板（已烧录main_integrated.c程序）
- [ ] 巡线小车底盘（含4个电机）
- [ ] 灰度传感器模块（2个，连接到GPIO19/20）
- [ ] 电机驱动板（支持PWM控制）
- [ ] TM1637数码管显示模块（连接到GPIO33/34）
- [ ] DHT11温湿度传感器（连接到GPIO38）
- [ ] RS485舵机（4个，ID 0x02-0x05）
- [ ] 黑线赛道（白底黑线，宽度约2cm）
- [ ] 电源（7.4V锂电池或12V适配器）
- [ ] USB数据线（用于日志监控）

### 2. 软件准备

```bash
# 1. 编译项目
cd /path/to/project
idf.py build

# 2. 烧录到ESP32
idf.py flash

# 3. 打开串口监控
idf.py monitor
```

### 3. 硬件连接检查

#### GPIO引脚连接表

| 功能 | GPIO | 连接对象 | 备注 |
|------|------|----------|------|
| 左灰度传感器 | GPIO19 | ADC2_CH9 | 模拟输入 |
| 右灰度传感器 | GPIO20 | ADC2_CH7 | 模拟输入 |
| 电机1正向 | GPIO2 | 电机驱动板IN1 | PWM输出 |
| 电机1反向 | GPIO3 | 电机驱动板IN2 | PWM输出 |
| 电机2正向 | GPIO4 | 电机驱动板IN3 | PWM输出 |
| 电机2反向 | GPIO5 | 电机驱动板IN4 | PWM输出 |
| 电机3正向 | GPIO6 | 电机驱动板IN5 | PWM输出 |
| 电机3反向 | GPIO7 | 电机驱动板IN6 | PWM输出 |
| 电机4正向 | GPIO8 | 电机驱动板IN7 | PWM输出 |
| 电机4反向 | GPIO9 | 电机驱动板IN8 | PWM输出 |
| TM1637 CLK | GPIO34 | 数码管CLK | 数字输出 |
| TM1637 DIO | GPIO33 | 数码管DIO | 数字I/O |
| DHT11 | GPIO38 | DHT11 DATA | 单总线 |
| UART0 TX | GPIO1 | RS485 TX | 串口发送 |
| UART0 RX | GPIO3 | RS485 RX | 串口接收 |

**重要提示**：
- 确保所有传感器和执行器共地（GND连接）
- 电机驱动板需要独立供电（7.4V或12V）
- ESP32供电使用USB或5V稳压电源
- 检查GPIO是否有短路或接触不良

## 测试流程

### 测试1：系统启动验证

**目标**：验证系统能够正常启动，所有模块初始化成功。

#### 步骤

1. 连接ESP32到电脑，打开串口监控
2. 按下ESP32的RESET按钮
3. 观察启动日志

#### 预期结果

```
========================================
STM32到ESP32移植项目 - 完整集成版本
========================================
开始初始化硬件外设...
初始化PWM...
初始化I2C...
初始化灰度传感器...
灰度传感器校准参数已设置
硬件外设初始化完成
开始初始化软件模块...
初始化转弯检测模块...
初始化PD控制器...
初始化转弯状态机...
软件模块初始化完成
开始系统自检...
ADC自检通过：左=XXXX, 右=XXXX
电机PWM自检通过
系统自检完成
配置看门狗定时器...
初始化定时器系统...
开始创建FreeRTOS任务...
创建ADC采样任务...
创建数码管显示任务...
创建舵机控制任务...
FreeRTOS任务创建完成
启动定时器系统...
========================================
系统启动完成！
========================================
```

#### 验证清单

- [ ] 所有初始化步骤都显示"完成"或"成功"
- [ ] 没有ERROR级别的日志
- [ ] ADC自检值在合理范围（0-4095）
- [ ] 系统启动完成消息出现
- [ ] 每5秒打印一次系统状态

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| ADC自检失败 | 传感器未连接 | 检查GPIO19/20连接 |
| 定时器初始化失败 | 代码错误 | 检查timer_system组件 |
| 看门狗初始化失败 | 配置错误 | 非致命错误，可继续测试 |

---

### 测试2：灰度传感器读取验证

**目标**：验证灰度传感器能够正确读取ADC值，区分黑线和白色区域。

#### 步骤

1. 将小车放置在白色区域
2. 观察串口日志中的ADC值
3. 将左传感器移动到黑线上
4. 观察左传感器ADC值变化
5. 将右传感器移动到黑线上
6. 观察右传感器ADC值变化

#### 预期结果

| 位置 | 左传感器ADC值 | 右传感器ADC值 |
|------|---------------|---------------|
| 白色区域 | 3500-4095 | 3500-4095 |
| 黑线上 | 1000-2000 | 1000-2000 |

#### 验证清单

- [ ] 白色区域ADC值接近4095
- [ ] 黑线上ADC值明显降低（<2500）
- [ ] ADC值变化响应迅速（<100ms）
- [ ] ADC错误计数保持为0或很低（<10次/5秒）
- [ ] 系统状态日志显示Timer0约5000次/5秒

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| ADC值始终为4095 | 传感器未连接 | 检查传感器供电和信号线 |
| ADC值始终为0 | 传感器短路 | 检查GPIO是否短路到GND |
| ADC值不稳定 | 干扰或接触不良 | 加滤波电容，检查连接 |
| ADC错误率高 | WiFi冲突 | 确保WiFi未启用 |

---

### 测试3：转弯检测功能验证

**目标**：验证转弯检测模块能够正确识别十字路口标记。

#### 步骤

1. 将小车放置在白色区域
2. 观察转弯状态日志：`转弯类型: 0, 转弯中: 0`
3. 同时将左右传感器移动到黑线上
4. 保持约15毫秒（连续15次1ms采样）
5. 观察转弯类型变化：`转弯类型: 3, 转弯中: 1`

#### 预期结果

- 双传感器同时检测到黑线连续15次后，转弯类型变为3（十字路口）
- 转弯进行中标志变为1（true）
- 状态机从TURN_IDLE (0) 转换到TURN_STOP (1)

#### 验证清单

- [ ] 单个传感器在黑线上不触发转弯
- [ ] 双传感器同时在黑线上触发转弯
- [ ] 转弯类型正确设置为3
- [ ] 转弯进行中标志正确设置为1
- [ ] 状态机状态从0变为1

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 无法触发转弯 | 阈值设置不当 | 调整LEFT_THRESHOLD和RIGHT_THRESHOLD |
| 误触发转弯 | 阈值过高 | 降低阈值或增加确认次数 |
| 转弯标志不清除 | 状态机未运行 | 检查Timer1是否正常工作 |

---

### 测试4：PD控制巡线功能验证

**目标**：验证PD控制器能够实现平滑的巡线控制。

#### 步骤

1. 将小车放置在黑线上（左右传感器分别在黑线两侧）
2. 确保转弯检测未触发（转弯类型=0）
3. 观察电机是否启动
4. 轻轻推动小车偏离黑线
5. 观察小车是否自动纠正回到黑线

#### 预期结果

- 小车能够沿着黑线平稳前进
- 偏离黑线时自动纠正
- 电机速度根据偏差动态调整
- 无明显震荡或抖动

#### 验证清单

- [ ] 小车能够启动并前进
- [ ] 偏离黑线时能够自动纠正
- [ ] 巡线过程平稳，无剧烈震荡
- [ ] Timer1中断约500次/5秒
- [ ] PD控制器正常执行（无ERROR日志）

#### 调试参数

如果巡线效果不佳，可以调整PD参数：

```c
// 在 main_integrated.c 的 init_software_modules() 中修改
pd_controller_set_params(
    700,    // 速度：增加到800可提高速度
    5.0f,   // Kp：增加可提高响应速度，但可能震荡
    12.0f   // Kd：增加可减少震荡，但响应变慢
);
```

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 电机不转 | PWM未输出 | 检查电机驱动板连接和供电 |
| 巡线震荡 | Kp过大 | 降低Kp或增加Kd |
| 响应迟钝 | Kp过小 | 增加Kp |
| 偏离黑线 | 校准参数错误 | 重新校准传感器 |

---

### 测试5：转弯执行功能验证

**目标**：验证六状态转弯状态机能够正确执行转弯动作。

#### 步骤

1. 在赛道上设置一个十字路口标记（黑线交叉）
2. 让小车沿着黑线前进
3. 当小车到达十字路口时，观察转弯动作
4. 记录状态机状态转换过程

#### 预期状态转换序列

```
TURN_IDLE (0) → 检测到十字路口
TURN_STOP (1) → 停车100ms
TURN_BACK (2) → 后退190ms
TURN_PHASE1 (3) → 右转直到左传感器离开黑线（最少250ms）
TURN_PHASE2 (4) → 右转直到右传感器找到新黑线
TURN_ADJUST (5) → 停车100ms
TURN_IDLE (0) → 转弯完成，继续巡线
```

#### 验证清单

- [ ] 检测到十字路口后立即停车
- [ ] 停车约100ms后开始后退
- [ ] 后退约190ms后开始转弯
- [ ] 转弯动作分两个阶段执行
- [ ] 找到新黑线后停车调整
- [ ] 转弯完成后恢复正常巡线
- [ ] 整个转弯过程约1-2秒

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 不执行转弯 | 转弯检测未触发 | 检查测试3 |
| 转弯不完整 | 转弯速度过慢 | 增加TURN_SPEED |
| 转弯过度 | PHASE1时间过长 | 减少TURN_PHASE1_MIN_TICKS |
| 找不到新黑线 | PHASE2逻辑错误 | 检查传感器阈值 |

---

### 测试6：数码管轮播显示验证

**目标**：验证数码管能够每5秒自动切换显示不同传感器数据。

#### 步骤

1. 观察数码管显示内容
2. 等待5秒，观察显示是否切换
3. 再等待5秒，观察是否切换到下一个模式
4. 循环观察3个显示模式

#### 预期显示模式

| 时间 | 显示模式 | 显示内容 | 说明 |
|------|----------|----------|------|
| 0-5秒 | 光照强度 | 0000-4095 | ADC原始值 |
| 5-10秒 | 温湿度 | TTHH | 温度2位+湿度2位 |
| 10-15秒 | 距离 | 0000-9999 | 单位：cm |
| 15-20秒 | 光照强度 | 0000-4095 | 循环回第一个模式 |

#### 验证清单

- [ ] 数码管能够正常显示数字
- [ ] 每5秒自动切换显示模式
- [ ] 光照强度显示合理（室内约1000-2000）
- [ ] 温湿度显示合理（温度20-30℃，湿度40-70%）
- [ ] 距离显示合理（根据实际距离）
- [ ] 显示刷新流畅，无闪烁

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 数码管不显示 | TM1637未连接 | 检查GPIO33/34连接 |
| 显示乱码 | I2C通信错误 | 检查I2C初始化 |
| 不切换模式 | display_task未运行 | 检查任务创建 |
| 温湿度显示0000 | DHT11读取失败 | 检查DHT11连接 |

---

### 测试7：舵机往复运动验证

**目标**：验证舵机能够每3秒在两组位置之间自动切换。

#### 步骤

1. 连接4个RS485舵机（ID 0x02-0x05）
2. 观察舵机初始位置
3. 等待3秒，观察舵机是否移动到新位置
4. 再等待3秒，观察舵机是否返回初始位置
5. 循环观察多次

#### 预期舵机位置

| 时间 | 位置组 | ID 0x02 | ID 0x03 | ID 0x04 | ID 0x05 |
|------|--------|---------|---------|---------|---------|
| 0-3秒 | 位置组1 | 800 | 650 | 850 | 750 |
| 3-6秒 | 位置组2 | 350 | 750 | 750 | 680 |
| 6-9秒 | 位置组1 | 800 | 650 | 850 | 750 |

#### 验证清单

- [ ] 4个舵机都能正常响应命令
- [ ] 每3秒自动切换位置
- [ ] 舵机运动平滑，无卡顿
- [ ] 位置切换准确
- [ ] 无舵机通信错误日志

#### 故障排除

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 舵机不动 | UART0被日志占用 | 关闭UART0日志输出 |
| 部分舵机不动 | ID不匹配 | 检查舵机ID设置 |
| 舵机抖动 | 供电不足 | 检查舵机电源 |
| 通信错误 | RS485连接错误 | 检查TX/RX连接 |

---

### 测试8：完整功能集成测试

**目标**：验证所有功能同时运行时系统稳定性。

#### 步骤

1. 将小车放置在完整的赛道上（包含直线和十字路口）
2. 启动系统，让小车自动巡线
3. 观察以下功能是否同时正常工作：
   - 巡线控制
   - 转弯检测和执行
   - 数码管轮播显示
   - 舵机往复运动
4. 运行至少5分钟，观察系统稳定性

#### 验证清单

- [ ] 小车能够沿着黑线平稳前进
- [ ] 遇到十字路口能够正确转弯
- [ ] 转弯后能够继续巡线
- [ ] 数码管持续轮播显示
- [ ] 舵机持续往复运动
- [ ] 无系统崩溃或重启
- [ ] 无看门狗超时
- [ ] ADC错误率保持低水平
- [ ] Timer中断频率稳定

#### 性能指标

| 指标 | 预期值 | 实际值 | 通过/失败 |
|------|--------|--------|-----------|
| Timer0中断频率 | 5000次/5秒 | _____ | _____ |
| Timer1中断频率 | 500次/5秒 | _____ | _____ |
| ADC错误率 | <50次/5秒 | _____ | _____ |
| 巡线速度 | 约0.5m/s | _____ | _____ |
| 转弯成功率 | >90% | _____ | _____ |
| 系统运行时间 | >5分钟 | _____ | _____ |

---

## 测试结果记录

### 测试环境

- **测试日期**：_______________
- **测试人员**：_______________
- **ESP32型号**：_______________
- **ESP-IDF版本**：_______________
- **赛道类型**：_______________
- **环境温度**：_______________

### 测试结果汇总

| 测试项 | 通过/失败 | 备注 |
|--------|-----------|------|
| 测试1：系统启动验证 | _____ | _____ |
| 测试2：灰度传感器读取验证 | _____ | _____ |
| 测试3：转弯检测功能验证 | _____ | _____ |
| 测试4：PD控制巡线功能验证 | _____ | _____ |
| 测试5：转弯执行功能验证 | _____ | _____ |
| 测试6：数码管轮播显示验证 | _____ | _____ |
| 测试7：舵机往复运动验证 | _____ | _____ |
| 测试8：完整功能集成测试 | _____ | _____ |

### 发现的问题

1. _______________________________________________
2. _______________________________________________
3. _______________________________________________

### 改进建议

1. _______________________________________________
2. _______________________________________________
3. _______________________________________________

### 测试结论

- [ ] 所有测试通过，系统功能正常
- [ ] 部分测试通过，需要修复问题后重新测试
- [ ] 测试失败，需要重大修改

---

## 下一步工作

完成硬件集成测试后，继续执行：

- **任务16.3**：执行长时间稳定性测试（24小时连续运行）
- **任务16.4**：编写转弯期间暂停检测和控制属性测试
- **任务15**：GPIO资源分配和冲突检测（可选）

## 附录

### A. 常用调试命令

```bash
# 查看实时日志
idf.py monitor

# 过滤特定标签的日志
idf.py monitor | grep "MAIN_INTEGRATED"

# 保存日志到文件
idf.py monitor > test_log.txt

# 重新烧录
idf.py flash

# 擦除flash后重新烧录
idf.py erase-flash
idf.py flash
```

### B. 日志级别调整

在 `main_integrated.c` 的 `app_main()` 开头添加：

```c
// 显示所有日志（包括DEBUG）
esp_log_level_set("*", ESP_LOG_DEBUG);

// 只显示错误日志（提高性能）
esp_log_level_set("*", ESP_LOG_ERROR);

// 只显示特定组件的日志
esp_log_level_set("MAIN_INTEGRATED", ESP_LOG_INFO);
esp_log_level_set("TIMER_SYSTEM", ESP_LOG_DEBUG);
```

### C. 参数调整快速参考

| 参数 | 位置 | 默认值 | 调整建议 |
|------|------|--------|----------|
| 巡线速度 | main_integrated.c | 700 | 增加到800提高速度 |
| Kp | main_integrated.c | 5.0 | 增加提高响应，减少降低震荡 |
| Kd | main_integrated.c | 12.0 | 增加减少震荡，减少提高响应 |
| 转弯确认次数 | turn_detector.h | 15 | 减少到10加快响应 |
| 左传感器阈值 | turn_detector.h | 2785 | 根据实际ADC值调整 |
| 右传感器阈值 | turn_detector.h | 2820 | 根据实际ADC值调整 |

祝测试顺利！🚗
